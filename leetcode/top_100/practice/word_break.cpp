#include <iostream>
using namespace std;

bool wordBreak(string s, vector<string>& wordDict) {
    // Do a BFS, at each point ,we examine the current starting position and the remaining strings
    // If we can break it up , then we do and add the remaining strings to the queue
    // Initial one is just 0

    return false;
}

int main() {

}